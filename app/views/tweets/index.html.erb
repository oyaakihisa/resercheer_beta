<div class="contents_row" >
  
  <div class=lucky_title>
  <h2>本日のラッキーresercher!</h2>
  <p>　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　※1万円進呈</p>
  </div>
  <div class="lucky_resercher" style="background-image: url(<%= @lucky_resercher.image %>);">

    <span class="name"><%= "池田純平" %></span>
    <p>色を変えれば印象は変わる</p>
  </div>
  
  <div class = "ikatui">
    <h2>ハードボイルド系resercher!</h2>
    <ul>
      <li>
        <% @tweets.each do |tweet| %>
          <%= render partial: "tweet", locals: { tweet: tweet } %>
        <% end %>
      </li>
    </ul>
  </div>
  
  <div class = "ikemen">
    <h2>内気系resercher!</h2>
    <ul>
      <li>
        <% @tweets.each do |tweet| %>
          <%= render partial: "tweet", locals: { tweet: tweet } %>
        <% end %>
      </li>
    </ul>
  </div>

  <%= paginate(@tweets) %>
</div>